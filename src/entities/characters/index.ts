import {
	getCharacters,
	postCharacter,
	putCharacter,
	deleteCharacter
} from './api'
import { currentCharacter, currentCharacters } from './model'
import type { CharacterData } from './type'

export {
	getCharacters,
	postCharacter,
	putCharacter,
	deleteCharacter,
	currentCharacter,
	currentCharacters,
	type CharacterData
}
